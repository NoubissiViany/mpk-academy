import Link from "next/link";
import { Check } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { formatPrice } from "@/config/product";
const plans = [{ name: "Free", price: "$0", items: ["10-question diagnostic", "Selected introductory lessons", "Basic competency result", "Limited quick practice"], href: "/diagnostic", cta: "Start diagnostic" }, { name: "Full TEF/TCF Program", price: formatPrice(), items: ["Complete curriculum", "Targeted Practice Mode", "Exam simulations", "Weakness and mistake tracking", "Progress and MPK Readiness", "Course completion certificate"], href: "/checkout", cta: "Get full access" }];
export const metadata = { title: "Pricing", description: "Simple one-time pricing for MPK Academy." };
export default function PricingPage() { return <div className="container-page py-20"><div className="mx-auto max-w-2xl text-center"><p className="eyebrow">Simple pricing</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Start free. Unlock the full program once.</h1><p className="mt-4 text-muted-foreground">No required subscription. No real payment is collected in this frontend MVP.</p></div><div className="mx-auto mt-12 grid max-w-4xl gap-5 md:grid-cols-2">{plans.map((plan, index) => <Card key={plan.name} className={index ? "border-primary ring-1 ring-primary" : ""}><CardContent className="pt-6"><p className="font-bold">{plan.name}</p><p className="mt-5 text-4xl font-black">{plan.price}</p><p className="mt-2 text-sm text-muted-foreground">{index ? "One-time purchase · CAD" : "No card required"}</p><ul className="mt-7 space-y-3">{plan.items.map((item) => <li key={item} className="flex gap-2 text-sm"><Check className="size-5 shrink-0 text-primary" />{item}</li>)}</ul><Button asChild variant={index ? "primary" : "secondary"} className="mt-8 w-full"><Link href={plan.href}>{plan.cta}</Link></Button></CardContent></Card>)}</div><p className="mx-auto mt-7 max-w-2xl text-center text-xs leading-5 text-muted-foreground">MPK Academy does not guarantee an exam score or immigration outcome. Readiness and practice results are internal learning indicators.</p></div>; }
