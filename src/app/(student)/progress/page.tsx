"use client";
import { BookOpenCheck, CheckCircle2, Goal, ListChecks, ShieldCheck, Target } from "lucide-react";
import { useApp } from "@/components/providers/app-provider";
import { MetricBar, PageHeader, Stat } from "@/components/shared";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { formatDate } from "@/lib/utils";
export default function ProgressPage() { const { state } = useApp(); const p = state.progress; return <><PageHeader eyebrow="Evidence over time" title="Progress you can act on." description="See what you have completed, how your core competencies are changing, and how this work connects to your exam goal." /><Card className="mb-5 border-primary/20 bg-primary/5"><CardContent className="flex flex-col gap-5 pt-5 sm:flex-row sm:items-center sm:justify-between sm:pt-6"><div className="flex gap-4"><span className="grid size-11 place-items-center rounded-full bg-primary text-primary-foreground"><Goal className="size-5" /></span><div><p className="text-sm text-muted-foreground">Your goal</p><h2 className="font-bold">{state.user?.goal.exam} — {state.user?.goal.target}</h2><p className="mt-1 text-xs text-muted-foreground">{state.user?.goal.targetDate ? formatDate(state.user.goal.targetDate) : "No target date set"}</p></div></div><Badge>Plan in progress</Badge></CardContent></Card><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Course completion" value={`${p.courseCompletion}%`} detail={`${p.completedLessonIds.length} lessons completed`} /><Stat label="Quiz performance" value={`${p.quizAverage}%`} detail="Across completed checkpoints" /><Stat label="Practice activity" value={`${p.practiceAnswered}`} detail={`${p.practiceAccuracy}% accuracy`} /><Stat label="Simulations" value={`${p.simulationsCompleted}`} detail={`${p.simulationAverage}% average`} /></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><Card><CardHeader><h2 className="text-lg font-bold">Competency performance</h2></CardHeader><CardContent className="space-y-5">{Object.entries(p.competencyScores).map(([id,value]) => <MetricBar key={id} label={id.replaceAll("-"," ").replace(/^./, (v) => v.toUpperCase())} value={value ?? 0} />)}</CardContent></Card><Card><CardHeader><h2 className="text-lg font-bold">Recent trend</h2></CardHeader><CardContent><div className="rounded-xl bg-primary/5 p-5"><p className="text-sm text-muted-foreground">Last four weeks</p><p className="mt-2 text-3xl font-bold text-primary">+4%</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Listening detail remains the clearest opportunity. Reading accuracy has stayed strong.</p></div><div className="mt-6 space-y-4">{[[BookOpenCheck,"Lessons","4 this month"],[Target,"Practice","28 answers"],[ShieldCheck,"Simulations","2 completed"],[ListChecks,"Reviews","3 mistake patterns"]].map(([Icon,label,value]) => { const I = Icon as typeof CheckCircle2; return <div key={String(label)} className="flex items-center gap-3"><I className="size-4 text-primary" /><span className="flex-1 text-sm">{String(label)}</span><strong className="text-sm">{String(value)}</strong></div>;})}</div></CardContent></Card></div></>; }
