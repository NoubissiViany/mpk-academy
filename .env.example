# Mock-only frontend settings. Never place secrets in NEXT_PUBLIC variables.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_MOCK_CHECKOUT_OUTCOME=success

# Future server-only integration placeholders (do not expose to browser code)
MPK_API_BASE_URL=
AUTH_SECRET=
PAYMENT_PROVIDER_SECRET=
QUESTION_PROVIDER_SECRET=
